<% flash.each do |type, message| %>
  <div class="alert alert-dismissible <%= alert_class type %>">
    <a href="#" class="close" data-dismiss="alert" aria-label="close">&times;</a>
    <strong><%= "#{type.capitalize} !" %></strong> <%= message %>
  </div>
<% end %>
